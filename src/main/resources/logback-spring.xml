<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console Appender (Logs to Console) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender (Overwrites app.log at startup) -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app.log</file>  <!-- The log file to be updated -->
        <append>false</append>  <!-- This ensures the file is overwritten on each startup -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root Logger Level (Logs INFO and above) -->
    <root level="info">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Spring Logs (Only show errors) -->
    <logger name="org.springframework" level="ERROR"/>

    <!-- Hibernate Logs (Disable SQL logs) -->
    <logger name="org.hibernate.SQL" level="OFF"/>
    <logger name="org.hibernate.type.descriptor.sql" level="OFF"/>

</configuration>
